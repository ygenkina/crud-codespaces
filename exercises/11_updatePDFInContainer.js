import { overwriteFile } from "@inrupt/solid-client";
import { fetch } from "@inrupt/solid-client-authn-browser";
// Using the methods above fill in the function below:
// Replace the Resume.pdf file located at resumeFileURL with the passed in file.
// Remember to consider if these should be authenticated calls or not.

export const updatePDFInContainer = async (file, resumeFileURL) => {
  alert("Complete exercise 11_updatePDFInContainer");  // delete this line

  try {
    
  } catch (e) {
    alert(e);
  }
};

export default updatePDFInContainer;

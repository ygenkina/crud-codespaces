import { deleteFile } from "@inrupt/solid-client";
import { fetch } from "@inrupt/solid-client-authn-browser";
// Using the imports above, fill in the function below:
// Delete the Resume file located at resumeFileURL.
// Remember to consider if these should be authenticated calls or not.

export const deletePDFInContainer = async (resumeFileURL) => {
  alert("Complete exercise 12_deletePDFInContainer"); // delete this line

  try {
    
  } catch (e) {
    alert(e);
  }
};

export default deletePDFInContainer;

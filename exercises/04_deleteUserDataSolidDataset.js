import { deleteSolidDataset } from "@inrupt/solid-client";
import { fetch } from "@inrupt/solid-client-authn-browser";
// Using the imports above, fill in the function below:
// Delete the SolidDataset located at datasetURL.
// Remember to consider if this should be an authenticated call or not.

export const deleteUserDataSolidDataset = async (datasetURL) => {
   alert("Complete exercise 04_deleteUserDataSolidDataset"); // delete this line
   
  try {

  } catch (e) {
    alert(e);
  }
};

export default deleteUserDataSolidDataset;

import { deleteContainer } from "@inrupt/solid-client";
import { fetch } from "@inrupt/solid-client-authn-browser";

// Using the imports above, fill in the function below:
// Delete the Container located at containerURL.
// Remember to consider if this should be an authenticated call or not.

export const deleteCRUDContainer = async (containerURL) => {
  alert("Complete exercise 08_deleteContainer"); // delete this line
  
  try {

  } catch (e) {
    alert(e);
  }
};

export default deleteCRUDContainer;

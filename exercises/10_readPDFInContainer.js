import { getFile } from "@inrupt/solid-client";
import { fetch } from "@inrupt/solid-client-authn-browser";
import { downloadFile } from "../utils";
// Using the imports above, fill in the function below:
// 1. Get the file located at resumeFileURL.
// 2. Call the downloadFile(file, fileName) function from utils.js to download the file.
// Remember to consider if these should be authenticated calls or not.

export const readPDFInContainer = async (resumeFileURL, fileName) => {
  alert("Complete exercise 10_readPDFInContainer"); // delete this line

  try {
    
  } catch (e) {
    alert(e);
  }
};

export default readPDFInContainer;
